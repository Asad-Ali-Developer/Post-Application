<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Your Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="styles/style.css">
    <style>
        *::selection{
            color: #18181b;
            background-color: #3b82f6;
        }
    </style>
</head>
<body>
    
    <div class="w-full min-h-screen bg-zinc-900 text-white">

        <div class="w-[85%] py-5 border-[1px] border-yellow-700 rounded-md flex flex-col items-center absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2">

                <h2 class="text-yellow-700 text-2xl text-center font-semibold mb-5">Edit Post</h2>

                    <!-- FORM STARTS HERE -->

                <form class="w-full px-2 flex flex-col items-center" action="/update/<%= post._id %>" method="post">
                    <textarea placeholder="What's on your mind?"
                        class="resize-none w-full p-2 bg-zinc-800 outline-none h-32 rounded-md"
                        name="content"><%= post.content %></textarea>

                    <input class="sendButton cursor-pointer w-full mt-3 bg-yellow-800 text-zinc-300 py-2 font-semibold rounded-md" type="submit" 
                        value="Update Post">
                </form>

                <!-- FORM ENDS HERE -->

            </div>
        </div>
    



    <script src="js/script.js"></script>
</body>
</html>